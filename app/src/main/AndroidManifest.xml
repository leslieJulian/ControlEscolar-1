<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.controlescolar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditarEspecialidad"></activity>
        <activity android:name=".Editar_plan_act" />
        <activity android:name=".Plan_Especialidades_activity" />
        <activity
            android:name=".Lista_especialidades_act"
            android:label="@string/title_activity_lista_especialidades_act"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Lista_plan_estudios"
            android:label="@string/title_activity_lista_plan_estudios"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Especialidad_act" />
        <activity android:name=".Plan_de_estudios" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>